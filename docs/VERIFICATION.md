# Локальная проверка

Дата: 18 сентября 2026 (America/Los_Angeles).

- PASS: 10 Django-тестов, отдельная временная база.
- PASS: миграции и сборка статики WhiteNoise.
- PASS: Chromium, каталог и паспорт, поиск, категории, фильтр цены,
  сортировка, выбор столбцов, раскрытие условий, пустое состояние, RU/EN.
- PASS: экраны 1440, 390 и 320 px; страница без горизонтального переполнения,
  название модели закреплено внутри прокручиваемой таблицы.
- PASS: базовые переходы при отключённом JavaScript.
- PASS: JavaScript-ошибок не обнаружено.
- PASS: 10 уникальных URL источников и сервисов ответили HTTP 200.
  Это проверка достижимости, не вход в платные API или выполнение моделей.
- PASS: изображения каталога и паспорта просмотрены; скриншоты в artifacts/.

Локальный сервер: http://127.0.0.1:18810, отдельный процесс Waitress.
Результаты не означают приёмку владельцем. Удалённый сервер, домен,
Cloudflare, нагрузка, платные API и реальное восстановление на сервере
не проверялись. Шаблоны развёртывания требуют SSH-проверки хоста.

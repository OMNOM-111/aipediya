# Выпуск release-2026-09-25-gsd-1-0 — «Глобальная поисковая доступность» (GSD-1.0)

- Дата (UTC): 2026-09-25, deploy 16:08Z.
- Commit / tag: `ba93f7ddf690f4a958405fbabfc65a59d3a9cfbf` /
  `release-2026-09-25-gsd-1-0`. Предыдущий Production: `ed103a3ff163`.
- Утверждение: владелец 2026-09-25 — выпуск полного текущего Local-состояния
  (GSD-1.0 + код catalog master + Local-only «История сайта»); отдельное явное
  разрешение на существующий SSH-канал только для операций AIpedia этого выпуска.
- Архив: `aipedia-code-ba93f7ddf690.zip`, sha256
  `5ed63075cb59e6bc97b05fe4e290c53b3a31202a043056aebb069dad8f8facc4`
  (совпал локально и на сервере), 290 файлов, без SQLite/секретов/XLSX/research.

## Шаги на сервере (только AIpedia)

1. Baseline: `/healthz` = `ed103a3`; integrity ok; FK violations 0; Models 304
   публичных из 763; Tools 138/138; ModelVersion 901; Offers 555; Evaluations 881;
   Access 1205; ContentTranslation 42 260; Revision 4 813; последняя миграция 0017.
2. Backup (online, sqlite backup API, integrity ok):
   `/srv/aipedia/backups/aipedia-before-release-2026-09-25-gsd-1-0-20260925T160739Z.sqlite3`
   + штатный `aipedia-before-code-20260925T160818Z.sqlite3`.
3. Dry-run `deploy_code_release.py … --publication-state data/release_state.json` — OK.
4. Deploy: остановлена только программа `aipedia`; `check`, `collectstatic`;
   `migrate` — `0018_discovery_outbox`; `sync_publication_state apply --apply` —
   models changed 0, tools changed 0 (304/138 = манифест); `aipedia` запущена;
   статус `deployed-origin-verified`, `copied_sqlite=false`. Предыдущий код —
   `/srv/aipedia/releases/before-code-20260925T160818Z`; после —
   `aipedia-after-code-20260925T160818Z.sqlite3`.
5. `aipedia-tunnel`, `aipedia-backup` не перезапускались; StratForge и чужие
   процессы/конфигурация/секреты не затрагивались. Переменные окружения
   Production не менялись (датасеты и IndexNow остаются выключенными).

## Боевая БД до → после

Все счётчики выше без изменений; добавлена пустая таблица
`catalog_discoveryevent`; integrity ok; FK violations 0; `BUILD.json` =
`ba93f7ddf690`.

## Проверки

- Тесты (gate): 216 PASS, `check` OK, `makemigrations --check` — нет изменений.
- Production GSD QA (`tools/gsd_production_qa.py`, 659 GET): **1596/1596 PASS** —
  корень и 21 locale path, Tools в 22 локалях, 12 legacy-редиректов (один 301 +
  200), SSR-карточки EN/RU/AR/ZH/UK/FA (модели и инструменты), canonical/hreflang/
  x-default/lang/dir/одна H1/JSON-LD, методика ×22, индекс и 9 подборок в 4
  локалях, тонкая подборка `music-models` — noindex, датасеты — 404 (выключены),
  пагинация (page 2/5 = 200, 6/abc = 404, сортировка — noindex без canonical,
  фрагмент — `X-Robots-Tag: noindex`), robots, sitemap index + 22 дочерних, все
  459 скрытых NEEDS_REVIEW — 404 и отсутствуют в sitemap.
  Первый прогон показал 13 ложных отказов из-за регистра имён заголовков в самом
  скрипте QA (Cloudflare отдаёт `location` в нижнем регистре; curl подтвердил
  верный ответ) — скрипт исправлен, регрессий сайта нет.
- `tools/gsd_public_check.py https://aipediya.com`: 34/34.
- Публичный sitemap: **10 032 `<loc>`** — карточки 9 724 (модели 6 688 +
  инструменты 3 036), подборки 198, индекс подборок 22, каталог моделей 22,
  каталог инструментов 22, методика 22, конфиденциальность 22.
- Визуально (headless Chrome, `artifacts/…/screens/PROD-*`): EN desktop, RU/AR
  карточки desktop (RTL), ZH подборка desktop, FA методика mobile, UK Tools mobile,
  JA карточка mobile. Local-only навигация и «История сайта» на Production не
  показываются.

## Внешняя активация

- Brave Search: запрос на повторную загрузку `https://aipediya.com/` принят формой
  `search.brave.com/submit-url` (принятие ≠ индексация).
- Google / Bing / Yandex / IndexNow / Naver / Baidu — требуют действий владельца,
  см. `docs/SEARCH_DISCOVERY.md` §10.

# Catalog master v013 — основной этап базы завершён, кандидат проверен

Дата: 2026-09-26 (UTC). Статус: **основной этап базы завершён; кандидат
проверен; ожидается визуальная приёмка владельца и отдельное разрешение на
выпуск**. Применено только к рабочему Local. Production не менялся (только
публичные GET-запросы), commit/push не выполнялись, одобрение владельца не
проставлялось. Поручения владельца 2026-09-25 и три уточнения 2026-09-26.

## 1. Одна книга

Действующая: `AI_CONTEXT/AIpediya_Model_Verification_Master.xlsx` (v013, схема
`/4`, sha256 `7e008132…203c`). Воспроизводимо собрана из v012 аудита (sha256
`3ea32a46…7208`) скриптом `tools/master_editorial_2026_09_26.py`, затем `import` с
рабочего Local и `check: OK`. Цепочка проектная книга → v001…v012 без потерь.
Резервные версии (v001–v012, черновики v013, проектная книга) — только копии в
`backups/catalog-master-finalize-20260925/` с SHA256SUMS; действующей книгой не
являются. Excel открывает все 12 листов.

## 2. Состав Local (ядро 304/138 → 309/140)

Полный список Record ID с номерами — `artifacts/catalog-master-v013-final/local_final_composition.json`
(sha256 `a1e91589…79e3`). Исходное ядро сверено 2026-09-26: Local = публичный
sitemap Production = 304/138.

* Скрыты как та же сущность, с 301 на канон (14): 12 датированных ID Claude
  (ссылка на карточку своего выпуска, не на «latest»), FLUX1.1 pro Raw (режим,
  та же цена), Nemotron 3 Super BF16 (формат весов; лицензия перенесена в канон).
* Не объединены, вопрос в Reason: Kimi K3 (max) (цены, контекст, лицензия и ECI
  есть только у неё), Grok Voice API (страница включает TTS/STT и их цены).
* Добавлены (21, On Production = NO): Gemma 1–3 (8), SAM, SAM 2, SAM 2.1,
  Whisper large-v3-turbo, Janus-1.3B, JanusFlow-1.3B, Janus-Pro-1B/7B,
  AlphaFold 3, MusicGen, AudioGen; инструменты ComfyUI, Google AI Studio.
* Jurassic-2 Light/Mid/Ultra = переименованные Large/Grande/Jumbo (AI21);
  закрытые Continue/Humanloop/Roo Code/Sora остаются с датированным статусом.

## 3. Исправления

* Нумерация по правилу владельца: модели 1–309, инструменты 1–113; резерв и 27
  недатированных инструментов без номера; прежние номера — в ревизиях.
* 30 описаний инструментов EN (текст master вместо шаблонного с ошибками) и RU
  вручную; 20 языков — машинный перевод F0 (1 020 пар, 124 660 символов).
* 11 оценок Artificial Analysis скрыты (Public=NO, в базе сохранены).
* Ссылка «Источник» 27 инструментов и 5 цен → официальные страницы из master;
  добавлены недостающие платформы 7 инструментов; у 6 доступов с собственным
  сервисом исправлены вид/место вычислений/URL (например, AutoGen и Semantic
  Kernel — локальный SDK, а не облачный веб-сервис).
* В самой книге исправлены 9 регрессий v012: ссылки доступа Llama/Qwen3-ASR,
  указывавшие на страницу другого размера модели.
* 119 приблизительных дат без `≈` — формат; 1 цена с единицей вне контракта.

## 4. Разбор неподдерживаемых расхождений (`unsupported_classified.csv`)

Из 162 расхождений 38 устранены (новые узкие операции и исправление книги).
Осталось 124:

| Влияние | Сколько | Что | Решение |
|---|---:|---|---|
| Публичное | 11 | ссылки доступа на общих сервисах (ElevenLabs API, Local-сервисы разработчиков, Llama 2 7B / Llama 3 8B) | очередь; блокер: правка общего сервиса изменила бы чужие карточки |
| Публичное | 16 | новые строки доступа (консоль Amazon Q, API Bedrock/Foundry/Vertex, веса Kimi/DeepSeek и др.) | очередь; блокер: нужен новый сервис, sync-local его не создаёт |
| Публичное | 3 | новые цены (Amazon Q Pro, Speech Engine burst, MiniMax STT) | очередь; блокер: новый сервис + проверка единицы/валюты |
| Служебное | 46 | ссылки-доказательства доступа | на сайте не выводятся |
| Служебное | 46 | справочные Billing Unit у стандартных единиц | по контракту не переносятся |
| Служебное | 2 | заголовок общего источника | ссылка совпадает |
| Резерв | 415 записей | поля непубличных записей | не синхронизируются |

Текущие публичные сведения этими 30 пунктами не искажаются: это дополнения и
уточнения. Контракт — `docs/CATALOG_MASTER.md`.

## 5. Подготовка выпуска (Production не изменялся)

* Нумерация больше не конфликтует с выпуском: `catalog_master release-plan`
  (план по Record ID с ожидаемыми старыми значениями, итоговыми номерами и
  составом) и `catalog_master apply-plan` (проверка → одна транзакция с
  ревизиями → контроль итога; уже применено → 0; расхождение → отказ без
  записи). Встроено в `deploy_code_release.py` (`--catalog-plan`,
  `--translations`) внутри его backup/отката; `sync_publication_state` остаётся
  контролем итоговых флагов и номеров.
* Выпускные данные: `data/release/v013/catalog_plan.json` (sha256 `fd6281a0…1c13`:
  21 создание, 289 обновлений, 46 цен, 11 оценок, 888 номеров, 7 платформ,
  6 сервисов), `data/release/v013/translations.json` (`4c8f3c13…d075`, 1 020
  переводов), `data/release_state.json` (`c13563fd…2c33`, 309/140).
* Окончательный кандидат (код, план, переводы, манифест, master) с полными
  SHA-256 — `artifacts/catalog-master-v013-final/FINAL_CANDIDATE.json` и
  `docs/EXECUTION_STATE.md` (архив не может содержать собственную сумму).
  Архив = базовый commit `b3116cf6` + файлы из `data/release/v013/CANDIDATE_FILES.txt`,
  у каждого sha256 в `BUILD.json`; параллельные правки истории сайта не входят.
* Испытанная база — **исходный снимок Local** (состояние до этой работы),
  сверенный с публичными данными Production построчно (304 + 138 строк списков);
  это не копия серверной БД. На ней: dry-run → pending; применение → 1 289
  записей, итог = план; `import_translations` 1 020; `sync_publication_state` →
  0 изменений; повтор → «already applied», 0. Итог совпал с рабочим Local:
  списки EN/DE (309/140), 1 240 страниц карточек (4 языка), 19 редиректов.
* Восстановление внутри `apply-plan`: подделанное ожидаемое значение → отказ,
  база побайтно та же; сбой посреди записи → откат транзакции.
* Восстановление всего выпуска (сценарий `release_process_recovery_script.py`:
  шаги и блок отката `deploy_code_release.py` до запуска приложения; supervisor
  и права Linux не воспроизводятся): сбой на шаге переводов после успешного
  `apply-plan` и сбой после переключения приложения → код побайтно вернулся к
  предыдущему приложению (commit Production `634778807b2e`), база — к исходной
  (схема 0018, та же контрольная сумма каталога, 304 публичных, те же счётчики
  переводов и ревизий), `migrate --check` предыдущего кода OK.
* Серверный preflight обязателен в будущем выпуске: несовпадение ожидаемых
  значений или внутренних ключей — остановка без принудительного применения и
  без перестроения плана на сервере.

## 6. Тесты

* Итоговый архив, распакованный вне проекта, чистое окружение (только
  системный PATH и git; без `AIPEDIA_*`/`PYTHON*`, без `.env.local`, без
  рабочего дерева; интерпретатор `.venv` без пакетов проекта, `-E -s`):
  `python -E -s manage.py test catalog --settings=aipedia.test_settings -v 2` —
  результат в `FINAL_CANDIDATE.json`. Пропуск: `fcntl` существует только в
  POSIX, тест блокировки выполняется на Linux-хосте.
* Два бывших ERROR `test_import_research` устранены: тесты больше не читают
  незакоммиченный `data/research/…json`, а создают контролируемый архив
  (13 записей всех типов, повтор имени у разных разработчиков, одно имя в двух
  типах, кириллица, BOM, объявленные счётчики) и проверяют точные счётчики,
  отказ при неверном объявленном счётчике, импорт только в staging, неизменность
  публичных таблиц и повторный импорт без дублей.
* Рабочее дерево Local: полный `catalog` — отдельный результат (другой набор
  тестов: в нём есть незакоммиченные тесты параллельного исполнителя), не
  подменяет результат архива.
* Выдача Local проверена по тексту/DOM/HTTP — это **не** полная визуальная приёмка.

## 7. Блокеры выпуска и отложенное

Блокеры выпуска: визуальная приёмка и явная команда владельца на этот
кандидат. Условия будущего выпуска (не технические блокеры): commit/tag ровно
этих файлов — если пакет после commit изменится, сверить его с испытанным и
проверить заново; обязательный серверный preflight `apply-plan` (серверная БД не
проверялась).

Отложено, не блокирует: 30 публичных дополнений из раздела 4; Kimi K3 (max) и
Grok Voice API; основание показа оценок AA; вычитка машинных переводов;
441 модель резерва (без повторного исследования).
